<div class ="row">
	<div class="span6 offset3">
		<div class="well">
			<p>
			<%= @pin.description %>
			</p>
			<p>
			<%= @pin.use.name %>
			</p>
			<% if current_use == @pin.use %>	
				<%= link_to 'Edit', edit_pin_path(@pin) %> |
			<% end %>	
			<%= link_to 'Back', pins_path %>
		
		</div>
	</div>
</div>			
